<languages />

{{DISPLAYTITLE:ACCEPT_KEYWORDS}}

{{Metadata|abstract=The ACCEPT_KEYWORDS variable informs the package manager which ebuilds' KEYWORDS values it is allowed to accept.}}

<code>ACCEPT_KEYWORDS</code> değişkeni, paket yöneticisinin ebuild'leri kullanırken hangi [[KEYWORDS]] değerlerini kabul etmesi gerektiğini belirler.

== Değişken nerede tanımlanır? ==

Değişken genellikle Gentoo [[profile]] içerisinde tanımlanır ancek kullanıcılar {{Path|[[:/etc/portage/make.conf]]}} dosyasında sistem için, {{Path|[[:/etc/portage/package.accept_keywords]]}} dosyasında her paket için ayrı ayrı değişkenin üzerine yazabilir hatta komut satırında tek kullanımlık dahi uygulayabilir.

{{Important/tr|<code>ACCEPT_KEYWORDS</code> değerini komut satırında geçici olarak tanımlamak sistemde kararsızlıklara yol açabileceği için genelde tavsiye edilmeyen bir yöntemdir.}}

== Kararlı ve kararsız keyword değerleri ==

Çoğu profilin <code>ACCEPT_KEYWORDS</code>'ü için öntanımlı değeri kendi mimarisidir. Örneğin <code>amd64</code> veya <code>arm</code> gibi. Bu sayede paket yönetici bir paketi kurarken yalnızca kendi mimarisine ait anahtar kelime bulunduran paketleri kullanacaktır. Kullanıcı dilerse kararlılığı tam olarak test edilmemiş paketleri de mimarinin önüne <code>~</code> karakteri koyarak kullanılabilir hale getirebilir. Örneğin:

{{FileBox|filename=[[:/etc/portage/make.conf]]|lang=bash|1=
ACCEPT_KEYWORDS="~amd64"
}}

Kararsız kanala (örn. <code>~amd64</code>) izin verilirken, kararlı kanalın da (<code>amd64</code>) ayrıca eklenmesine gerek yoktur. <code>ACCEPT_KEYWORDS</code> değeri yalnızca tek değişkene izin vermektedir.

Eğer ayar sistem genelinde yapılmayacaksa, her paket için ayrı olarak {{Path|package.accept_keywords}} dosyasında veya dizininde tanımlanabilir:

{{FileBox|filename=[[:/etc/portage/package.accept_keywords]]|1=
# oyunlar
games-fps/doomsday ~amd64
}}

<code>ACCEPT_KEYWORDS</code> içindeki normal değerlerin dışında {{Path|package.accept_keywords}} üç özel değeri daha destekler<ref>Gentoo Portage, [https://dev.gentoo.org/~zmedico/portage/doc/man/portage.5.html Manual page for Portage]. Retrieved on January 30th, 2015.</ref>:
* <code>*</code> — paket herhangi bir mimaride kararlı durumda ise görülebilir
* <code>~*</code> — paket herhangi bir mimaride kararsız durumda ise görülebilir
* <code>**</code> — paket her zaman görülebilir (<code>KEYWORDS</code> değeri yok sayılır)

The last choice is useful for live package versions (which are svn/git/mercurial/… versions of packages, supported through what is called a live ebuild and which don't have a <code>KEYWORDS</code> variable).

== References ==


{{reflist}}

[[Category:Portage]]
