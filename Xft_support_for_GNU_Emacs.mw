== Enabling antialiased fonts for Emacs ==

=== Installation and setup ===
{{Note|Font anti-aliasing using the Xft library and FreeType fonts is available with Emacs 23 or later.}}
First, set the appropriate USE flags – you ''must'' have the <code>xft</code> flag.
{{RootCmd|echo "app-editors/emacs xft" >> /etc/portage/package.use}}

Now it's time to install Emacs:
{{Emerge|app-editors/emacs}}

You can now use XFT fonts in emacs; try emerging {{Package|media-fonts/dejavu}} first.
{{Cmd|emacs --font 'DejaVu Sans Mono-12'}}

If you're happy with this as your default font, set it in your [[X resources|{{Path|~/.Xresources}}]]:
{{Cmd
|echo "Emacs.font: DejaVu Sans Mono-12" >> ~/.Xresources
|xrdb -merge ~/.Xresources
}}


==== Lucid toolkit ====

When Emacs was built with the Lucid toolkit (i.e. with the <code>athena</code> or <code>Xaw3d</code> USE flags), the font of the menubar can be set using the following in your {{Path|~/.Xresources}}:
{{CodeBox|1=
Emacs.pane.menubar.font: DejaVu Sans-12
}}


==== Motif toolkit ====

For Emacs built with the Motif toolkit (i.e. with the <code>motif</code> USE flag enabled), anti-aliased fonts can be enabled using the XFT font renderer in Motif. Make sure that {{Package|x11-libs/motif}} was built with the <code>xft</code> flag, and add for example the following to your {{Path|~/.Xresources}}, in order to set the font globally:
{{CodeBox|1=
Emacs.*.renderTable: emacs
Emacs.*.renderTable.fontType: FONT_IS_XFT
Emacs.*.renderTable.fontName: DejaVu Sans
Emacs.*.renderTable.fontSize: 10
}}
More specific control of resources is also possible, e.g., the following will set a bold font for the menubar:
{{CodeBox|1=
Emacs.pane.menubar.*.renderTable.fontStyle: bold
}}

Don't forget to load the resources:
{{Cmd|xrdb -merge ~/.Xresources}}

=== Resources ===
For more details on Emacs with XFT pretty fonts, see:
* [https://www.emacswiki.org/emacs/XftGnuEmacs XftGnuEmacs] on the Emacs wiki

{{Migrated|originalauthors=Thomas Nichols, Ulrich Müller, Christian Faulhammer}}
[[Category:Editor]]
