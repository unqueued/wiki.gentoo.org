{{Stub}}
{{DISPLAYTITLE:tcpdump}}
{{Metadata|abstract=<tt>tcpdump</tt> is a network monitoring and data acquisition tool. It is capable of sniffing packets and "dumping" information.}}
{{InfoBox stack
|{{InfoBox homepage|http://www.tcpdump.org/|header=true}}
|{{InfoBox wikipedia|Tcpdump}}
|{{InfoBox github|the-tcpdump-group/tcpdump}}
}}

<tt>tcpdump</tt> is a command-line network monitoring and data acquisition tool. It is capable of sniffing packets and "dumping" information.

__TOC__

== Installation ==

=== USE flags ===

{{USEflag|package=net-analyzer/tcpdump|desc=Optional USE flags for {{Package|net-analyzer/tcpdump}}:
|drop-root
|ipv6
|samba
|smi
|ssl
|suid
|test
}}

=== Emerge ===

Install <tt>tcpdump</tt>:

{{Emerge|net-analyzer/tcpdump}}

== Configuration ==

In order for normal users to run <tt>tcpdump</tt> they need to be added to the tcpdump group. Do this by using the <tt>usermod</tt> command where <code><username></code> is user's username:

{{RootCmd|usermod -a -G ftp <username>}}

== Usage ==

=== Invocation ===

If a standard user has been added to the <code>tcpdump</code> group the following command should be executable without needing root permissions:

{{RootCmd|tcpdump -h|output=<pre>
Usage: tcpdump [-aAbdDefhHIJKlLnNOpqRStuUvxX#] [ -B size ] [ -c count ]
                [ -C file_size ] [ -E algo:secret ] [ -F file ] [ -G seconds ]
                [ -i interface ] [ -j tstamptype ] [ -M secret ] [ --number ]
                [ -Q in|out|inout ]
                [ -r file ] [ -s snaplen ] [ --time-stamp-precision precision ]
                [ --immediate-mode ] [ -T type ] [ --version ] [ -V file ]
                [ -w file ] [ -W filecount ] [ -y datalinktype ] [ -z command ]
                [ -Z user ] [ expression ]
</pre>}}

== See also ==

* [[Wireshark]] - A free and open-source packet analyzer.

== External resources ==

* http://www.tcpdump.org/manpages/pcap.3pcap.html - The tcpdump man page hosted on the web.

[[Category:Software]]
