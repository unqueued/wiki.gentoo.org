<noinclude>{{DISPLAYTITLE:CONFIG_PROTECT}}<languages /></noinclude>
<!-- This page is transcluded, please keep the "summary paragraphs" "example" "more information" structure in place.
When extending further, use the noinclude and includeonly options instead. -->

La variable <var>CONFIG_PROTECT</var> contiene una lista de directorios delimitados por espacios en los que Portage no copia sobreescribiendo ciegamente con la nueva versión de archivos, sino que los trata como archivos de configuración en los que el usuario debe comprobar manualmente si los cambios son necesarios y validos o no. Los subdirectorios de los directorios listados se pueden excluir utilizando la variable <var>CONFIG_PROTECT_MASK</var>.

The variable has a sane default setting handled by the Portage installation and the users' Gentoo [[Profile (Portage)|profile]]. It can be extended through the system environment (which is often used by applications that update the variable through their {{Path|/etc/env.d}} file) and the users' {{Path|[[:/etc/portage/make.conf]]}} setting.

{{FileBox|filename=/etc/portage/make.conf|title=Ejemplo de uso de CONFIG_PROTECT en make.conf|lang=bash|1=
CONFIG_PROTECT="/var/bind"
}}

See also the [[Handbook:AMD64/Working/EnvVar|Environment variables]] chapter in the Gentoo Handbook.<noinclude>[[Category:Portage]]</noinclude>
