{{Stub}}

The {{Path|/etc/conf.d}} directory generally contains initscript configuration files. This directory becomes important when using Gentoo's OpenRC [[init system]] (which is enabled by default).

== See also ==

* {{See also|OpenRC}}
* [[:/etc/init.d]] - The directory that contains all available initscripts installed on a system.
