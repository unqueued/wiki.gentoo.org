The {{Path|package.properties}} file allows <code>ACCEPT_PROPERTIES</code> to be augmented for single (individual) packages.

== Format ==

* Comment lines begin with <code>#</code> (no inline comments)
* One DEPEND atom per line followed by additional properties
