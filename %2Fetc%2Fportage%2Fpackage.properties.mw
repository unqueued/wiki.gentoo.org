The {{Path|package.properties}} file allows <var>ACCEPT_PROPERTIES</var> to be augmented for single (individual) packages.

== Format ==

* Comment lines begin with <code>#</code> (no inline comments).
* One DEPEND atom per line followed by additional properties.
