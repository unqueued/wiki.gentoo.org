<languages />
<div class="mw-translate-fuzzy">
{{InfoBox stack
|{{InfoBox homepage|https://www.kernel.org|header=true}}
|{{InfoBox package|sys-kernel/gentoo-sources}}
|{{InfoBox wikipedia|Linux kernel}}
|{{InfoBox github|torvalds/linux}}
|{{InfoBox gitweb|https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git|raw=true}}
}}
</div>

<div class="mw-translate-fuzzy">
Linux 内核是操作系统的核心，提供了程序访问硬件的接口。内核包含了绝大多数的设备驱动。
</div>

<div class="mw-translate-fuzzy">
尽管 [https://gentoo.org/get-started/about/ Gentoo 是一个基于 Linux 或 FreeBSD 的自由操作系统]，并且 FreeBSD 有[https://www.freebsd.org/doc/en_US.ISO8859-1/books/handbook/kernelconfig.html 它自己的内核],因为一些原因，这篇文章只介绍 Linux 内核的相关内容。
</div>

== 安装 ==

=== USE 旗标 ===

要构建内核，必须先安装内核源码归档。对于桌面系统来说，我们推荐使用 {{Package|sys-kernel/gentoo-sources}}。它由 Gentoo 开发者维护，提供修复安全漏洞，功能问题，以及兼容少见的系统架构所需的补丁。

{{USEflag|package=sys-kernel/gentoo-sources}}

=== 安装（Emerge） ===

安装 {{Package|sys-kernel/gentoo-sources}}:

{{Emerge|sys-kernel/gentoo-sources}}

== 可选的其它内核 == 

The [[Kernel/Overview|Kernel sources overview]] article provides details on most, if not all, of the kernel sources packages available in Gentoo.

=== 查看所有可选项 ===

所有内核的详细名单和简单的描述都可以通过{{c|emerge}}查询:

{{Emerge|params=--search|"%@^sys-kernel/.*sources"}}

<div class="mw-translate-fuzzy">
== 配置 ==
</div>

=== Configuration ===

; [[genkernel|Automatic configuration]]: {{c|genkernel}}是一个用来自动化构建内核和initramfs的软件。genkernel的目的是内核构建过程中帮助用户。

; [[Kernel/Configuration|手工配置]]: 手工配置使得用户——通过一些努力——可以创建一个定制的内核配置。

; [[Kernel/Gentoo Kernel Configuration Guide|Gentoo kernel configuration guide]]: Gentoo内核配置指南。

; [[Security_Handbook/Kernel_security|Kernel security]]: 说明内核的安全性

跟上面的类似，Kernel Seeds 帮助用户，通过一些努力，在一个已有的内核配置 {{Path|.config}} 的基础上创建一个定制的内核配置。跟上面的类似，Kernel Seeds 帮助用户，通过一些努力，在一个已有的内核配置 {{Path|.config}} 的基础上创建一个定制的内核配置。

=== Upgrade ===

; [[Kernel/Upgrade|Upgrade]]: 通过已有的内核配置升级新内核。

; [[Kernel/Migrate 2.4 to 2.6|2.4 to 2.6 Migration]]: 从2.4内核到2.6内核的迁移指南。由于Linux内核代码库已经明显高于2.6，这些迁移步骤不再相关，但可能会帮助管理员，了解非常有用的东西。

=== Removal ===

; [[Kernel/Removal|Removal]]: 如何移除旧的内核。

== Troubleshooting ==

=== In-kernel configuration support ===

See the [[Kernel/IKCONFIG Support|IKCONFIG support]] sub-article.

=== Kernel command-line parameters ===

When booting from a bootloader, the Linux kernel can accept command-line parameters to change its behavior. This can aid in troubleshooting the kernel at boot time, to blacklist a certain module that should not loading, etc.

Kernel.org has a [https://www.kernel.org/doc/html/latest/admin-guide/kernel-parameters.html nicely formatted list of available kernel command-line parameters] to review.

Specifically, the following command-line parameters may be helpful when booting Gentoo:

* <code>earlyprintk=</code>
* <code>module_blacklist=</code>
* <code>nomodule</code>
* <code>loglevel=</code>
* <code>rootdelay=</code>

== 参考 ==

<div class="mw-translate-fuzzy">
* [[:Category:Kernel|The kernel category]] - 在wiki-上的所有与内核相关的文章。
* [[:Category:Hardware|The hardware category]] - 具有相关内核配置的硬件堆栈列表
</div>

== 外部资源 ==

<div class="mw-translate-fuzzy">
* [http://planet.kernel.org/ planet.kernel.org/] - Blogs related to the Linux kernel.
* [https://kernelnewbies.org/ kernelnewbies.org/] - "A site for aspiring Linux kernel developers who work to improve their Kernels and more experienced developers willing to share their kernel knowledge."
</div>


[[Category:Kernel]]
[[Category:Meta]]
