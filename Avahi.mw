{{stub}}
{{InfoBox stack
|{{InfoBox homepage|http://www.avahi.org/|header=true}}
|{{InfoBox wikipedia|Avahi_(software)|header=true}}
}}

'''avahi''' is a zeroconf service discovery & publishing program.

== Installation ==
=== USE flags ===
{{USEflag|package=net-dns/avahi|desc=Optional USE flags for {{Package|net-dns/avahi}}
|autoipd
|bookmarks
|dbus+yes
|doc
|gdbm+yes
|gtk+yes
|gtk3
|howl-compat
|introspection+yes
|ipv6+yes
|mdnsresponder-compat
|mono
|nls+yes
|python
|qt4
|selinux
|utils
}}

=== Emerge Avahi server ===
Install {{Package|net-dns/avahi}}:

{{emerge|net-dns/avahi}}

Be sure to add the service to the default runlevel for the server daemon to start on system boot. Do so with the <tt>rc-update</tt> command:
{{RootCmd|rc-update add avahi-daemon default}}

Start the service (without restarting) with the <tt>rc-service</tt> command:
{{RootCmd|rc-service avahi-daemon start}}

=== Emerge Avahi client ===
Gentoo contains the client component of Avahi that enables one to test this service. 

Install {{Package|sys-auth/nss-mdns}}:

{{emerge|sys-auth/nss-mdns}}

Add the appropriate mdns into the hosts line in {{Path|/etc/nsswitch.conf}}, An example line looks like:

{{FileBox|filename=/etc/nsswitch.conf|1=
hosts:       files mdns4_minimal [NOTFOUND=return] dns mdns4
}}

To also enable IPv6 support, change the line to:

{{FileBox|filename=/etc/nsswitch.conf|1=
hosts:       files mdns_minimal [NOTFOUND=return] dns mdns
}}

Once this is installed it should be possible to ping the hostname of the machine appending <code>.local</code>. For example: 

{{RootCmd|ping hostname.local}}

== See also ==

== External resources ==

[[Category:Daemons]]
