<languages />

{{InfoBox stack
|{{InfoBox homepage|http://www.vim.org|header=true}}
|{{InfoBox wikipedia|Vim (text editor)}}
}}

{{Metadata|abstract=Vim is a text editor based on vi. It can be used from the command line or as a standalone application with graphical user interface.}}

'''Vim''' ('''V'''i '''im'''proved) is a text editor based on [[Wikipedia:Vi|vi]]. It can be used from the command-line or as a standalone application with a graphical user interface.

== 설치 ==

=== USE flags ===

{{USEflag|package=app-editors/vim|desc=Optional USE flags for {{Package|app-editors/vim}}:
|X
|acl+yes
|cscope
|debug
|gpm
|lua
|luajit+++Use dev-lang/luajit instead of dev-lang/lua
|minimal
|nls+yes
|perl
|python
|python_targets_python2_7+++Build with Python 2.7
|python_targets_python3_3+++Build with Python 3.3
|python_targets_python3_4+++Build with Python 3.4
|racket+++Enable support for Scheme using dev-lang/racket
|ruby
|tcl
|vim-pager
}}

=== vim 명령줄 ===

X 윈도 시스템 지원 기능이 필요하지 않으면 {{Package|app-editors/vim}} 꾸러미를 설치하십시오:

{{Emerge|app-editors/vim}}

=== Vim GUI ===

To install Vim with both the ncurses-based interface ({{Path|/usr/bin/vim}}) as well as the graphical interface (for the X Window System - {{Path|/usr/bin/gvim}}), install {{Package|app-editors/gvim}}:

{{Emerge|app-editors/gvim}}

There is also an experimental [[Qt]] interface called {{Package|app-editors/vim-qt}} which can also be installed.

=== 확장 기능 ===

''app-vim'' 분류 항목에는 여러가지 문법 정의, 플러그인, 다른 Vim 관련 기능이 있습니다.

Use <tt>emerge</tt> or <tt>eix</tt> to get an overview of available packages in the ''app-vim'' category:

{{Cmd|emerge --search "%@^app-vim"
|eix -cC app-vim}}

== 설정 ==

Vim can be configured on a per-user basis or through a system-wide configuration file. 

The system wide settings are set in {{Path|/etc/vim/vimrc}} whereas the user-specific configuration is located inside the user's home directory at {{Path|~/.vimrc}}.

== 사용법 ==

=== 시작하기 ===

Vim은 30분 정도의 시간이 필요한 내장형 따라하기 문서가 있습니다. {{c|vimtutor}} 명령으로 시작하십시오:

{{Cmd|vimtutor}}

== 요령 ==

=== 명령행에서 ex 또는 ed처럼 Vim 사용 ===

It is possible to use Vim for one-liners — commands that can be used in scripts or on the command line to make changes in an unattended manner.

다음 명령에서는 {{Path|file.txt}} 파일의 각각의 줄 앞에 <code>#</code>을 추가합니다:

{{Cmd|vim -c ":%s/^/#/g" -c ":x" file.txt}}

What happens is that Vim interprets the passed on commands (through the <code>-c</code> option). The first command is Vim's substitution command (which is very similar to <tt>sed</tt>'s), the second one is Vim's instruction to save and exit the editor.

=== 파일 인코딩 바꾸기 ===

파일 인코딩을 [[UTF-8/ko|UTF-8]]로 바꾸려면 다음 명령을(Vim 명령 모드에서) 사용하십시오:

{{Cmd|e ++enc{{=}}utf8|prompt=:|color=orange}}

앞에서 보여드린 요령처럼 마찬가지로 명령줄에서도 처리할 수 있습니다:

{{Cmd|vim -c ":wq! ++enc{{=}}utf8" file.txt}}

== 추가 참조 ==

* [[Vim/Guide|Vim Guide]] - Explains how to use Vim in greater detail.

== 외부 자료 ==

* [http://vimdoc.sourceforge.net/ Vim 문서] 는 HTML, PDF, PS 형식의 설명서 (aka ":help" and Free VIM OPL Book), 자주 묻는 질문, 절차 설명, 따라하기 내용이 있습니다.
* [http://www.vim.org/scripts/index.php VIM Scripts/Plugins]
* [http://shop.oreilly.com/product/9780596529833.do Learning the vi and Vim Editors, 7th Edition]&nbsp; O'Reilly&nbsp; Print ISBN: 978-0-596-52983-3, Ebook ISBN: 978-0-596-15935-1

[[Category:Editor]]
