<noinclude><languages/></noinclude>Network device names <code>eth0</code>, <code>wlan0</code>, etc. as provided by the kernel could be changed on boot (see {{c|dmesg}}) by the {{Path|/lib/udev/rules.d/80-net-name-slot.rules}} udev rule.

为了保持经典的命名，这个规则可以用 {{Path|/etc/udev/rules.d}}  目录中用同样命名的空文件覆盖：

{{RootCmd
|touch /etc/udev/rules.d/80-net-name-slot.rules
}}
