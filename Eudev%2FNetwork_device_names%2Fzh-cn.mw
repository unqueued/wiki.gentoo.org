<noinclude><languages/></noinclude>内核提供的网络设备名称  <code>eth0</code>, <code>wlan0</code> 等可以通过  {{Path|/lib/udev/rules.d/80-net-name-slot.rules}} udev 规则在启动时更改参见 {{c | dmesg}}。

为了保持经典的命名，这个规则可以用 {{Path|/etc/udev/rules.d}}目录中用同样命名的空文件覆盖：

{{RootCmd
|touch /etc/udev/rules.d/80-net-name-slot.rules
}}
