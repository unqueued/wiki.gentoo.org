{{InfoBox stack
  |{{InfoBox wikipedia|Universal Serial Bus|header=true}}
}}
This article describes the setup of USB (Universal Serial Bus) controllers.

== Installation ==

=== Hardware detection ===
To choose the right driver, first detect the used USB controllers. The [[Hardware detection#lspci|lspci]] utility works nicely for this task:
{{RootCmd|lspci {{!}} grep --color -i usb}}

=== Kernel Configuration ===
The following kernel options will also need activated:
{{KernelBox|<pre>
Device Drivers --->
    [*] USB support  --->
        <*>   Support for Host-side USB

        Select a USB HCI driver, e.g.:
        <*> xHCI HCD (USB 3.0) support (xhci-hcd)
        <*> EHCI HCD (USB 2.0) support (ehci-hcd)
        <*> OHCI HCD support (ohci-hcd)
        <*> UHCI HCD (most Intel and VIA) support (uhci-hcd)
</pre>}}

=== Portage Configuration ===
To check if the <code>usb</code> [[USE flag]] is activated, run:
{{Cmd|euse -I usb}}

Desktop related profiles have it activated by default. See
{{Cmd|grep usb /usr/portage/profiles/targets/desktop/make.defaults}}

Should you however need to activate it, this can be done either in {{Path|[[:/etc/portage/package.use]]}} or in {{Path|[[:/etc/portage/make.conf]]}}. 

{{FileBox|filename=/etc/portage/make.conf|lang=bash|1=
USE="... usb ..."
}}

After setting this be sure to update the system with the <code>--changed-use</code> option so the changes take effect:
{{Emerge|params+=--changed-use --deep|@world}}

=== Emerge ===
Install the {{Package|sys-apps/usbutils}} package, if it is not already installed by adding the <code>usb</code> USE flag and re-running emerged with <code>--changed-use</code>:

{{Emerge|sys-apps/usbutils}}

== See also ==

* [[USB/Guide|Gentoo Linux USB Guide]]
* [[Kernel/Gentoo_Kernel_Configuration_Guide#USB_host_controllers|Gentoo Kernel Configuration Guide#USB host controllers]]

[[Category:Interfaces]]
