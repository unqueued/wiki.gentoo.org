<languages />
{{InfoBox stack
|{{InfoBox homepage|http://www.kernel.org|header=true}}
|{{InfoBox wikipedia|Linux kernel}}
}}

Linux カーネルは、オペレーティングシステムの中核であり、プログラムがハードウェアにアクセスする経路を提供しています。カーネルは、ほとんどのデバイスドライバも含んでいます。

== インストール ==

=== USE フラグ ===

To create a kernel, it is necessary to install the kernel source code first. The Gentoo recommended kernel sources for a desktop system are, of course, {{Package|sys-kernel/gentoo-sources}}. These are maintained by the Gentoo developers, and patched to fix security vulnerabilities, functional problems, as well as to improve compatibility with rare system architectures.

{{USEflag|package=sys-kernel/gentoo-sources|desc=Optional USE flags for {{Package|sys-kernel/gentoo-sources}}:
|build++no
|deblob
|symlink
}}

=== Emerge ===

Now install {{Package|sys-kernel/gentoo-sources}}:

{{Emerge|sys-kernel/gentoo-sources}}

== Alternative kernels == 

Portage ツリーには、他にも多種多様なカーネルソースがあります:

* {{Package|sys-kernel/vanilla-sources}} - The official, non-patched Linux kernel sources. These sources are left as is, they do not contain any additional patches and are therefore not supported by Gentoo support or developers.
* {{Package|sys-kernel/hardened-sources}} - Gentoo sources with security enhancements.

=== Sources overview ===

; [[Kernel/Overview|Kernel sources overview]]: Most, if not all, of the kernel sources packages available in Gentoo are detailed in the kernel sources overview article. Navigate there for further knowledge on specific kernel sources available in the Portage tree.

=== Searching all alternatives ===

A full list of kernel sources with short descriptions can be found by searching with <tt>emerge</tt>:

{{Emerge|params=--search|sources}}

== コンフィグレーションのヘルプ ==

; [[genkernel|Automatic configuration]]: <tt>genkernel</tt> is a tool used to automate the build process of the kernel and initramfs. The goal of genkernel is to help users through the kernel building process.

; [[Kernel/Configuration|Manual configuration]]: Manual configuration enables the user, with some effort, to create a custom-fit kernel configuration.

; [[Kernel/Gentoo Kernel Configuration Guide|Gentoo カーネルコンフィグレーションガイド]]: Gentoo のカーネルコンフィグレーションガイド

; [[Kernel/Upgrade|アップグレード]]: 既存のコンフィグレーションを流用して新たなカーネルにアップグレードする手順。

; [[Kernel/Removal|削除]]: 古いカーネルを完全に削除するための手順。

; [[Kernel/Configuration/Kernel_Seeds|Kernel Seeds]]: Like the option above, Kernels Seeds help the user, with some effort, create a custom-fit kernel configuration using an existing {{Path|.config}} as a base.

; [[Kernel/Migrate 2.4 to 2.6|2.4 to 2.6 Migration]]: Migration guide from Linux 2.4 to Linux 2.6. Since the Linux kernel codebase has significantly moved past 2.6, these migration steps are no longer relevant, but may be useful for helping administrators understand.

== 参考 ==

* [[:Category:Kernel|The kernel category]] - All the kernel related articles on the wiki.

== External resources ==

* [http://planet.kernel.org/ planet.kernel.org/] - Blogs related to the Linux kernel.
* [http://www.kernelhub.org/ kernelhub.org/] - A site dedicated to kernel statistics.
* Torvalds' kernel source repositories:
** [https://git.kernel.org/cgit/ Kernel source repository (Kernel.org).]
** [https://github.com/torvalds/linux kernel Kernel source repository (GitHub mirror).] 

[[Category:Kernel]]
