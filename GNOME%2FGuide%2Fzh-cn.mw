<languages />
{{Dated}}
{{Stub}}
{{Metadata|文摘=经常使用GNOME桌面环境。本指南试图描述GNOME的所有方面,包括安装、配置、使用、和更多!}}
{{InfoBox stack
|{{InfoBox homepage|http://www.gnome.org/|header=true}}
|{{InfoBox wikipedia}}
}}
一个经常使用的环境是GNOME。本指南试图描述GNOME的所有方面,包括安装、配置、使用、和更多!

== 什么是 GNOME? ==

=== 项目 ===

 [http://www.gnome.org GNOME project] 是一个免费的软件项目，它致力于GNOME的发展,Unix / Linux桌面套件和开发平台。 [http://foundation.gnome.org GNOME Foundation] 协调发展和GNOME项目的其他方面。 

=== 软件 ===

GNOME桌面环境和开发平台。这片自由软件的桌面选择几个行业领导者。有趣的是同时针对业务用户、家庭用户和开发人员开发。 

===社区 ===

像任何大的自由软件项目,GNOME有着广泛的用户和开发基地。[http://planet.gnome.org GnomePlanet] 是一个受GNOME黑客和贡献者欢迎的博客汇聚，而 [http://developer.gnome.org Developer.Gnome.Org] is for the GNOME developers. [http://library.gnome.org/users/ GNOME Library] 包含一个巨大的GNOME终端用户资源列表。 [http://worldofgnome.org/ World of GNOME] 也是一个受欢迎的GNOME-related新闻汇聚，

== 安装GNOME ==

=== 需要什么？ ===

{{Important|第一次先看看说明指导：[[Xorg/Guide|Xorg guide]] 来设置一个X环境}}

在安装之前GNOME,编辑系统的USE是一个好主意。确保<code>X</code>, <code>gtk</code>，和 <code>gnome</code> USE位于{{Path|/etc/portage/make.conf}}的变量。 If support for <code>dbus</code> 增加他到 USE flags ([[dbus]]是 Gnome 广泛使用的系统消息总线)，如果不需要 KDE支持， 移除USE 配置中的<code>qt4</code> 和 <code>kde</code> ， USE 变量可以添加一个负号 (<code>-</code>) 在他们前面。看下面的例子，这是正确使用减号的方法。

{{FileBox|filename=/etc/portage/make.conf|title=Example USE flags for a GNOME desktop environment|lang=bash|1=
USE="-qt4 -kde X dbus gtk gnome"
}}

增加 <code>branding</code> ，USE flag提供了一个可爱的Gentoo的启动画面而不是默认的GNOME启动画面：

{{RootCmd|echo "gnome-base/gnome-session branding" >> /etc/portage/package.use}}

{{Note|当使用 <code>desktop/gnome</code> profile, 这些 USE flags将为你设置。 可以通过运行检查系统的profile<kbd>eselect profile list</kbd>. 可以作为root用户运行{{c|eselect profile set <profile-name>}} 修改。 一定要更改 "<profile-name>" 的数字或名称所需的 profile.}}

一旦完成,就由emerge GNOME来安装GNOME: 

{{RootCmd|emerge gnome-base/gnome}}

“最小化”GNOME安装,{{c|emerge gnome-light}}，该选项提供了一个轻量级的GNOME安装没有额外的工具,但是一个完整的GNOME安装提供了所以你可能需要安装额外的软件包。

{{RootCmd|emerge gnome-base/gnome-light}}

{{Note| {{Package|gnome-base/gnome-light}}包不会依赖{{Package|gnome-extra/gnome-screensaver}}包, 这是需要锁定屏幕。注意屏幕没有它不会锁定。}}

这将需要一段时间，所以你可能要开始阅读这些妈妈给你买的，但你却从来没有打开的书。准备好了？很好，现在更新的环境变量： 

{{RootCmd|env-update && source /etc/profile}}

接下来剩余的服务和用户组将被清除。 

{{RootCmd|/etc/init.d/dbus start
|rc-update add dbus default}}

验证 {{c|plugdev}} 组存在与否。 如果存在,建议让每一个GNOME用户组的成员,都加入这个组，但这一步是可选的(该组是不常见的)。 

{{RootCmd|getent group plugdev|output=<pre>
plugdev:x:104:
</pre>}}

取代 <code><username></code> 在接下来的命令与每一个GNOME用户的用户名:

{{RootCmd|gpasswd -a <username> plugdev}}

===第一印象===

It is time to take a look at what was just built. Exit the root shell and log in as a regular user. The next step is to configure the session manager to run GNOME when the the {{c|startx}} command is invoked (see [[Xorg/Guide#Using_startx|using startx]] in the Xorg guide for more information).

{{Cmd|echo "exec gnome-session" > ~/.xinitrc}}

Starting with gnome-base/gnome-session-2.26.2, 用户需要先考虑<code>XDG_MENU_PREFIX</code>变量在使用GNOME菜单 {{Path|~/.xinitrc}} 方法启动桌面 If {{Path|~/.xinitrc}}不被使用它会自动处理;无需额外的配置。 

{{Cmd|sed -i '1i\export XDG_MENU_PREFIX{{=}}gnome-' ~/.xinitrc}}

现在开始运行安装的图形环境{{c|startx}}: 

{{Cmd|startx}}

如果一切顺利GNOME应该高兴地提供一个问候。祝贺你设置GNOME!

你可以进一步配置GNOME。请看这篇文章 [[GNOME/Guide#See_also]] 获取额外的定制信息。

== 另请参阅 ==

* [[GNOME/gdm|GDM article]] 介绍如何设置GNOME显示管理器自动开机，从而进入一个图形环境（而不是通过命令来启动GNOME桌面<tt>startx</tt>）。

== 外部资源 ==

* http://worldofgnome.org/ - The World of GNOME blog.

* https://github.com/dantrell/gentoo-project-gnome-without-systemd - GNOME without Systemd


[[Category:GNOME]]

{{Migrated|originalauthors=Sven Vermeulen, Lars Strojny, nightmorph}}
