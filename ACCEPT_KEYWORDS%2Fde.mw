<languages />

{{DISPLAYTITLE:ACCEPT_KEYWORDS}}

{{Metadata|abstract=Die <code>ACCEPT_KEYWORDS</code>-Variable informiert den Paket-Manager darüber, welche ebuild [[KEYWORDS]] Werte akzeptiert werden.}}

Die <var>ACCEPT_KEYWORDS</var>-Variable informiert den Paket-Manager darüber, welche ebuild <var>[[KEYWORDS]]</var> Werte akzeptiert werden.

== Wo wird die Variable definiert? ==

Die Variable wird normalerweise durch das Gentoo [[Profile (Portage)]] gesetzt, kann jedoch durch den Benutzer in der Datei {{Path|[[:/etc/portage/make.conf]]}} global, oder für jedes Paket einzeln in der Datei oder dem Verzeichnis {{Path|[[:/etc/portage/package.accept_keywords]]}}, oder sogar auf der Kommandozeile überschrieben werden.

{{Important|Generell ist es kein guter Stil und keine gute Idee, die <var>ACCEPT_KEYWORDS</var>-Variable auf der Befehlszeile zu überschreiben, da dies nicht persistent ist und zu ungewollten Resultaten bzw. ungewolltem Verhalten des Paket-Managers führen kann.}}

== Stabile und instabile Schlüsselworte ==

In den meisten Profilen ist der Standartwert der <code>ACCEPT_KEYWORDS</code>-Variablen die Architektur selbst, wie zum Beispiel <var>amd64</var> oder <code>arm</code>. In diesen Fällen akzeptiert der Packet-Manager nur ebuilds mit der <var>KEYWORD</var>-Variable der angegebenen Architektur. Falls man ebuilds, die noch nicht für den produktiven Einsatz bestimmt sind installieren und benutzen möchte, kann man die gleichen Architekturen – allerdings mit dem Prefix <code>~</code> – hinzufügen:

{{FileBox|filename=[[:/etc/portage/make.conf]]|lang=bash|1=
ACCEPT_KEYWORDS="~amd64"
}}

Man sollte nicht das stable keyword (<code>amd64</code>) und das testing keyword (<code>~amd64</code>) gleichzeitig angeben, da <var>ACCEPT_KEYWORDS</var> eine inkrementelle Variable ist.

Falls die Einstellungen nicht systemweit vorgenommen werden sollen, kann dies für jedes Paket individuell in der {{Path|package.accept_keywords}} Datei oder Verzeichnis erfolgen:

{{FileBox|filename=[[:/etc/portage/package.accept_keywords]]|1=
# - Spiele
games-fps/doomsday ~amd64
}}

Zusätzlich zu den normalen Werten aus <var> ACCEPT_KEYWORDS</var>, unterstützt {{Path|package.accept_keywords}} drei spezielle Kürzel<ref>Gentoo Portage,[https://dev.gentoo.org/~zmedico/portage/doc/man/portage.5.html Manual page for Portage]. Abgerufen am 30. Januar 2015.</ref>:

* <code>*</code> — Package is visible if it is stable on any architecture.
* <code>~*</code> — Package is visible if it is in testing on any architecture.
* <code>**</code> — Package is always visible (<var>KEYWORDS</var> are ignored completely).

Die letzte Auswahl ist nützlich für Live-Paketversionen (welches SVB/Git/Mercurial/… Versionen des Paketetes sind, die im sogenannten "live ebuild" unterstüzt werden und keine <var>KEYWORDS</var> Variable haben).

== Siehe auch ==

* [[Knowledge Base:Accepting a keyword for a single package]]

== Referenzen ==


{{reflist}}

[[Category:Portage]]
