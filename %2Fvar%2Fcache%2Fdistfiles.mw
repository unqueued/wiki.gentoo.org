'''{{Path|/usr/portage/distfiles}}''' is the default directory [[Portage]] uses to store source code files. These are typically in a compressed package format. This directory can be altered by changing the value of the <var>DISTDIR</var> varabile in {{Path|[[:/etc/portage/make.conf|make.conf]]}}.

== See also ==

* [[eclean]] - A utility for cleaning up Portage's {{Path|distfiles}} and {{Path|packages}} directories.

[[Category:Portage]]
