<languages />
<div class="mw-translate-fuzzy">
{{InfoBox stack
|{{InfoBox homepage|https://www.kernel.org|header=true}}
|{{InfoBox package|sys-kernel/gentoo-sources}}
|{{InfoBox wikipedia|Linux kernel}}
|{{InfoBox github|torvalds/linux}}
|{{InfoBox gitweb|https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git|raw=true}}
}}
</div>

<div class="mw-translate-fuzzy">
Ядро Linux - основной компонент операционной системы, который позволяет программам взаимодействовать с оборудованием. Ядро содержит в себе большую часть драйверов.
</div>

<div class="mw-translate-fuzzy">
Хотя [https://gentoo.org/get-started/about/ Gentoo это свободная операционная система на базе Linux или FreeBSD], а FreeBSD имеет [https://www.freebsd.org/doc/en_US.ISO8859-1/books/handbook/kernelconfig.html свое собственное ядро], по практическим соображениям, эта статья относится к ядру Linux.
</div>

== Установка ==

=== USE-флаги ===

Чтобы собрать ядро, необходимо сначала установить его исходный код. Для настольных систем Gentoo рекомендует исходный код ядра {{Package|sys-kernel/gentoo-sources}}. Он поддерживается разработчиками Gentoo и пропатчен для исправления уязвимостей, функциональных проблем, а также для улучшения совместимости с редкими системными архитектурами.

{{USEflag|package=sys-kernel/gentoo-sources}}

=== Emerge ===

Теперь установите {{Package|sys-kernel/gentoo-sources}}:

{{Emerge|sys-kernel/gentoo-sources}}

== Ещё ядра == 

The [[Kernel/Overview|Kernel sources overview]] article provides details on most, if not all, of the kernel sources packages available in Gentoo.

=== Поиск всех альтернатив ===

* Полный список исходных кодов ядра с кратким описанием может быть получен при поиске в {{c|emerge}}:

{{Emerge|params=--search|"%@^sys-kernel/.*sources"}}

<div class="mw-translate-fuzzy">
== Конфигурация ==
</div>

=== Configuration ===

; [[genkernel/ru|Автоматическая настройка]]: {{c|genkernel}} - утилита для автоматизации процесса сборки ядра и образа initramfs. Задача genkernel заключается в помощи пользователю во время сборки ядра.

; [[Kernel/Configuration/ru|Ручная настройка]]: Ручная настройка позволяет, с некоторыми усилиями, создать собственную, более подходящую, конфигурацию ядра.

; [[Kernel/Gentoo Kernel Configuration Guide/ru|Статья по конфигурации ядра в Gentoo]]: Статья по настройке ядра в Gentoo.

; [[Security_Handbook/Kernel_security|Kernel security]]: Инструкции по безопасности ядра.

; [[Kernel/Configuration/Kernel_Seeds|Основа ярда]]: как и утилита выше "Основа" ядра помогает пользователю создать собственную конфигурацию ядра, используя уже существующий конфигурационный файл {{Path|.config}} за основу.

=== Upgrade ===

; [[Kernel/Upgrade/ru|Обновление]]: Инструкции по обновлению ядра с использованием существующей конфигурации.

; [[Kernel/Migrate 2.4 to 2.6|Переход с 2.4 на 2.6]]: Статья о миграции с Linux 2.4 на Linux 2.6.

=== Removal ===

; [[Kernel/Removal/ru|Удаление]]: Инструкции для полного удаления старых ядер.

== Troubleshooting ==

=== In-kernel configuration support ===

See the [[Kernel/IKCONFIG Support|IKCONFIG support]] sub-article.

=== Kernel command-line parameters ===

When booting from a bootloader, the Linux kernel can accept command-line parameters to change its behavior. This can aid in troubleshooting the kernel at boot time, to blacklist a certain module that should not loading, etc.

Kernel.org has a [https://www.kernel.org/doc/html/latest/admin-guide/kernel-parameters.html nicely formatted list of available kernel command-line parameters] to review.

Specifically, the following command-line parameters may be helpful when booting Gentoo:

* <code>earlyprintk=</code>
* <code>module_blacklist=</code>
* <code>nomodule</code>
* <code>loglevel=</code>
* <code>rootdelay=</code>

== Смотрите также ==

<div class="mw-translate-fuzzy">
* [[:Category:Kernel|The kernel category]] - все статьи о ядре на wiki.
* [[:Category:Hardware|The hardware category]] - список категорий аппаратного обеспечения с сопутствующими настройками ядра.
</div>

== Ссылки ==

<div class="mw-translate-fuzzy">
* [http://planet.kernel.org/ planet.kernel.org/] - блоги связанные с ядром Linux.
* [https://kernelnewbies.org/ kernelnewbies.org/] - "Сайт для начинающих разработчиков ядра Linux, которые работают над тем, чтобы улучшить свои ядра, а так же более опытных разработчиков, желающих поделиться своими знаниями о ядре."
</div>


[[Category:Kernel]]
[[Category:Meta]]
