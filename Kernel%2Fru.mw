<languages />
{{InfoBox stack
|{{InfoBox homepage|http://www.kernel.org|header=true}}
|{{InfoBox wikipedia|Linux kernel}}
|{{InfoBox github|torvalds/linux}}
|{{InfoBox gitweb|https://git.kernel.org/cgit/|raw=true}}
}}

Athough [https://gentoo.org/get-started/about/ Gentoo is a free operating system based on either Linux or FreeBSD] and FreeBSD has [http://www.freebsd.org/doc/en_US.ISO8859-1/books/handbook/kernelconfig.html its own kernel], for practical reasons, this article refers to the Linux kernel.

Ядро Linux - основной компонент операционной системы, который позволяет программам взаимодействовать с оборудованием. Ядро содержит в себе большую часть драйверов.

== Установка ==

=== USE флаги ===

To create a kernel, it is necessary to install the kernel source code first. The Gentoo recommended kernel sources for a desktop system are, of course, {{Package|sys-kernel/gentoo-sources}}. These are maintained by the Gentoo developers, and patched to fix security vulnerabilities, functional problems, as well as to improve compatibility with rare system architectures.

{{USEflag|package=sys-kernel/gentoo-sources|desc=Необязательные USE флаги для {{Package|sys-kernel/gentoo-sources}}:
|build++no
|deblob
|symlink
}}

=== Установка ===

Теперь установите '''gentoo-sources''':

{{Emerge|sys-kernel/gentoo-sources}}

== Ещё ядра == 

Существуют альтернативные варианты исходных текстов ядра в дереве Portage:

* {{Package|sys-kernel/vanilla-sources}} - Официальные, не патченные исходники. Исходники оставлены так как есть, без добавления дополнительных патчей. Поэтому они не поддерживаются разработчиками Gentoo.
* {{Package|sys-kernel/hardened-sources}} - исходники ядра Linux с исправлениями безопасности Gentoo

== Краткое описание исходников ==

Большинство, если не все, пакеты ядер Linux доступны в Gentoo и детально описаны в кратких обзорных статьях. Перейдите по ссылке для того, чтобы узнать больше о специфичных исходниках ядер, доступных в основном репозитории Gentoo

=== Поиск всех альтернатив ===

* Полный список с кратким описанием может быть получен при поиске в emerge:

{{Emerge|params=--search|sources}}

== Конфигурация ==

; [[genkernel|Автоматическая настройка]]: <tt>genkernel</tt> - утилита для автоматизации процесса сборки ядра и образа initramfs. Задача genkernel заключается в пользователю в течение процесса сборки ядра.

; [[Kernel/Configuration|Ручная настройка]]: Ручная настройка позволяет вам создать собственную конфигурацию ядра для ваших нужд.

; [[Kernel/Gentoo Kernel Configuration Guide|Статья по конфигурации ядра в Gentoo]]: Статья по настройке ядра в Gentoo.

; [[Kernel/Upgrade|Обновление]]: Шаги для обновления на новое ядро с использованием существующей конфигурации:

; [[Kernel/Removal|Удаление]]: Шаги для полного удаления старых ядер.

; [[Kernel/Configuration/Kernel_Seeds|Основа ярда]]: как и утилита выше "Основа" ядра помогает пользователю создать собственную конфигурацию ядра, используя уже существующий конфигурационный файл {{Path|.config}} как основу

; [[Kernel/Migrate 2.4 to 2.6|Переход с 2.4 на 2.6]]: Статья о миграции с Linux 2.4 на Linux 2.6.

== Смотри также ==

* [[:Category:Kernel|The kernel category]] - все статьи, связанные с ядрами на wiki.

== External resources ==

* [http://planet.kernel.org/ planet.kernel.org/] - Blogs related to the Linux kernel.
* [http://www.kernelhub.org/ kernelhub.org/] - A site dedicated to kernel statistics.

[[Category:Kernel]]
