{{Metadata|abstract=phpBB is open-source bulletin board software, an internet forum package using the PHP scripting language.}}

{{InfoBox stack
|{{InfoBox homepage|http://www.phpbb.com/|header=true}}
|{{InfoBox wikipedia|phpBB}}
|{{InfoBox project|Webapps}}
}}

'''phpBB''' is open-source bulletin board software, an internet forum package using the [[PHP]] scripting language.

== Install ==

Before installing '''phpBB''', it is recommended to peruse the configuration options for [[Apache]], [[MariaDB]]/[[MySQL]], and [[PHP]] as well.

=== Use flags ===

{{USEflag|package=www-apps/phpBB}}

Especially important is to enable PHP support for {{package|www-servers/apache}} by installing {{package|dev-lang/php}} with the <code>apache2</code> USE-flag, as well as to enable database support via the <code>mysql</code> USE-flag.

=== Emerge ===

{{Emerge|{{Package|www-apps/phpBB}}}}

== Configuration ==

=== Enabling Apache PHP support ===

{{Important|1=With >=[https://packages.gentoo.org/packages/app-eselect/eselect-php app-eselect/eselect-php-0.8.1], the <var>APACHE2_OPTS</var> variable changed to <code>-D PHP</code> instead of <code>-D PHP5</code>. This allows for future major versions to flow smoothly.}}

{{FileBox|filename=/etc/conf.d/apache2|title=Enabling the PHP module|lang=bash|1=
APACHE2_OPTS="... -D PHP"
}}

== Creating a database ==

Refer to [[MySQL/Startup Guide]] and [[MySQL/Guide]] for detailed instructions.

{{RootCmd|/etc/init.d/mysql start}}

{{RootCmd
|mysql -u root -p
|output=<pre>
mysql> create database if not exists <database name> default character set utf8 collate utf8_unicode_ci;
mysql> create user '<username>'@'localhost' identified by '<password>';
mysql> grant select, insert, update, delete, create, drop, index, alter on phpbb.* to 'phpbb'@'localhost' identified by '<password>';
mysql> quit
</pre>}}

== Completing the phpBB install ==

{{RootCmd|/etc/init.d/apache2 start}}

Browsing to http://localhost/phpBB (<code>https</code> for SSL installs) should now bring up the phpBB introduction page with overview and install tabs, which will guide through the rest of the installation process (assuming the package is installed under <code>/var/www/localhost/htdocs/phpBB</code>).

{{Important|1=The forum will be accessible to administrators only, until the <code>/var/www/localhost/htdocs/phpBB/install</code> directory is removed or renamed (provided that the package lives under <code>/var/www/localhost/htdocs/phpBB</code>.}}

[[Category:Web application]]
