<languages />

{{InfoBox stack
|{{InfoBox homepage|http://www.vim.org|header=true}}
|{{InfoBox wikipedia|Vim (text editor)}}
}}

{{Metadata|abstract=Vim es un editor de texto basado en vi. Se puede utilizar desde la línea de órdenes o como una aplicación independiente con interfaz gráfica de usuario.}}

'''Vim''' ('''V'''i '''im'''proved) es un editor de texto basado en [[Wikipedia:Vi|vi]]. Se puede utilizar desde la línea de órdenes o como una aplicación independiente con una interfaz gráfica de usuario.

== Instalación ==

=== Ajustes USE ===

{{USEflag|package=app-editors/vim|desc=Ajustes USE opcionales para {{Package|app-editors/vim}}:
|X
|acl+yes
|cscope
|debug
|gpm
|lua
|luajit+++Utiliza dev-lang/luajit en lugar de dev-lang/lua
|minimal
|nls+yes
|perl
|python
|python_targets_python2_7+++Se construye con Python 2.7
|python_targets_python3_3+++Se construye con Python 3.3
|python_targets_python3_4+++Se construye con Python 3.4
|racket+++Habilita el soporte para Scheme utilizando dev-lang/racket
|ruby
|tcl
|vim-pager
}}

=== Línea de órdenes de Vim ===

Si no se necesita soporte del sistema X Window, simplemente se puede instalar {{Package|app-editors/vim}}:

{{Emerge|app-editors/vim}}

=== Vim con GUI ===

To install Vim with both the ncurses-based interface ({{Path|/usr/bin/vim}}) as well as the graphical interface (for the X Window System - {{Path|/usr/bin/gvim}}), install {{Package|app-editors/gvim}}:

{{Emerge|app-editors/gvim}}

There is also an experimental [[Qt]] interface called {{Package|app-editors/vim-qt}} which can also be installed.

=== Extensiones ===

La categoría ''app-vim'' ofrece una gran cantidad de definiciones adicionales de sintaxis, plugins y otros elementos relacionados con Vim.

Utilice <tt>emerge</tt> o <tt>eix</tt> para obtener una vista general de los paquetes disponibles en la categoría ''app-vim'':

{{Cmd|emerge --search "%@^app-vim"
|eix -cC app-vim}}

== Configuration ==

Vim can be configured on a per-user basis or through a system-wide configuration file. 

The system wide settings are set in {{Path|/etc/vim/vimrc}} whereas the user-specific configuration is located inside the user's home directory at {{Path|~/.vimrc}}.

== Usage ==

=== Comenzar ===

Vim tiene un tutorial incorporado que requiere alrededor de treinta minutos de lectura. Comience utilizando la orden <tt>vimtutor</tt>:

{{Cmd|vimtutor}}

== Consejos y trucos ==

=== Usar Vim como ex o ed desde la línea de órdenes ===

Es posible utilizar Vim para ficheros de una línea, órdenes que se pueden utilizar en la línea de órdenes para hacer cambios sin intervención del usuario.

Por ejemplo, la siguiente orden añade <code>#</code> al principio de cada línea en el fichero {{Path|fichero.txt}}:

{{Cmd|vim -c ":%s/^/#/g" -c ":x" fichero.txt}}

Lo que ocurre es que Vim interpreta las órdenes que se han pasado (mediante la opción the <code>-c</code>). La primera orden es la orden de sustitución de Vim (que es muy similar a la de <tt>sed</tt>), la segunda es la instrucción de Vim para guardar y salir del editor.

=== Cambiar la codificación del fichero ===

Para cambiar la codificación del fichero a [[UTF-8/es|UTF-8]], utilice la siguiente orden (en el modo de órdenes de Vim):

{{Cmd|e ++enc{{=}}utf8|prompt=:|color=orange}}

Tal y como se muestra en el truco anterior, es posible hacer esto también desde la línea de órdenes:

{{Cmd|vim -c ":wq! ++enc{{=}}utf8" fichero.txt}}

== Ver también ==

* [[Vim/Guide|Vim Guide]] - Explains how to use Vim in greater detail.

== Recursos externos ==

* [http://vimdoc.sourceforge.net/ Documentación de Vim] Incluye manuales (también conocidos como ":help" y Libros de Vim personales libres ), FAQS, HOWTO's, Tutoriales, en formatos HTML, PDF y PS.
* [http://www.vim.org/scripts/index.php Scripts/Plugins de VIM]
* [http://shop.oreilly.com/product/9780596529833.do Learning the vi and Vim Editors, 7th Edition]&nbsp; O'Reilly&nbsp; Print ISBN: 978-0-596-52983-3, Ebook ISBN: 978-0-596-15935-1

[[Category:Editor]]
